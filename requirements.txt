# Dub Studio backend — THIN wrapper deps only.
# torch / torchaudio + the dub-engine ML stack (Parakeet ONNX, Sortformer/NeMo, llama-cpp-python
# fork for Gemma, Qwen3-TTS + triton-windows) are installed SEPARATELY by install.bat per the
# proven engine venv recipe — never pinned blindly here. See PACKAGING.md.
fastapi
uvicorn[standard]
python-multipart
numpy
# the engine lives in ./dub-engine (this repo); install.bat installs it editable: pip install -e dub-engine
