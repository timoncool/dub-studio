# dub-engine runtime deps — PROVEN pins for the engine ML stack (cu128 / cp311).
# torch/torchaudio/torchvision, llama-cpp-python (JamePeng cu128 wheel) and triton-windows are
# installed SEPARATELY in install.bat (explicit wheels). Sortformer (NeMo) lives in its OWN
# sub-venv (.venv-sortformer) — see install.bat. This file is the MAIN-env engine stack.
numpy==2.4.6
scipy==1.17.1
soundfile==0.14.0
librosa==0.11.0
pydantic>=2,<3
# ASR — Parakeet TDT int8 ONNX
onnx-asr==0.11.0
onnxruntime-gpu==1.26.0
# MT + vision context model is llama-cpp (Gemma GGUF) — wheel installed in install.bat
# TTS — Qwen3-TTS (nf4 + triton)
transformers==4.57.3
accelerate==1.12.0
bitsandbytes==0.49.2
sentencepiece==0.2.1
safetensors==0.8.0
einops==0.8.2
torchdiffeq==0.2.5
omegaconf==2.3.1
# Source separation (vocals/music)
audio-separator==0.44.2
# On-screen text OCR + layout
rapidocr==3.8.4
opencv-python==4.13.0.92
pillow==12.2.0
# Speaker reference picking
Resemblyzer==0.1.4
# Model download + media
huggingface_hub==0.36.2
hf-xet==1.5.1
ffmpeg-python==0.2.0
